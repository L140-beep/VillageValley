extends Node

enum PLANTS {
	BEET
}
enum ITEMS {
	SEED_BEET,
	BEET,
	BUCKET
}

const SEEDS = [ITEMS.SEED_BEET]

const FRUITS = [ITEMS.BEET]

enum GARDEN_BED_STATES {
	EMPTY,
	BUSY
}

enum PROFESSIONS {
	FARMER,
	
}

enum ZONES {
	HOME,
	HOUSE,
	OUTSIDE
}
